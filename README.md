# Music Gallery Demo

Static demo that loads `manifest.json` and shows artists, images and playable tracks.

- No account or login required.
- About section intentionally left blank for user content.

To run, serve this folder with any static server (e.g., `python -m http.server`).
